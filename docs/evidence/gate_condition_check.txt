RETIRED EVIDENCE — do not cite as the current gate-condition result.

The original run reported 3/5 proposals and treated two zero-length provider
completions as ordinary "NOTHING" results. That mixed a token-budget defect
with model behavior and is not an acceptable measurement.

The corrected live five-run transcript, recorded after adding an explicit
max_tokens and rejecting empty completions, is:

    docs/evidence/negative_control.txt

Current result: 5/5 proposals, run count 5, CP_MAX_TOKENS=24576.
